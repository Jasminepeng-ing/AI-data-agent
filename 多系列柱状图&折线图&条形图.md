当前AI数据分析agent输出的柱状图(bar)、横向条形图(barh)、折线图(line)只支持单列数据(y_col),无法展示多系列数据。

现在需要给柱状图(bar)、横向条形图(barh)、折线图(line)
三种图表加入多系列(multi-series)展示能力。

【问题描述】
当前 Schema 只有:
- x_col: X 轴字段(一列)
- y_col: Y 轴字段(一列)
这导致 Agent 无法回答:
"对比 2017 年各月葡萄、西瓜、荔枝、菠萝的销售量趋势"
"展示各州在 Q1/Q2/Q3/Q4 的订单量对比"

需要支持:
- y_cols: Y 轴字段列表(多列),如 ["Q1", "Q2", "Q3", "Q4"]
- color_col: 分组字段(长格式数据场景),如 "product_category"

【数据格式说明】

多系列数据有两种常见格式,都要支持:
格式 A — 宽格式(Wide Format,每列是一个系列):
┌──────┬──────┬──────┬──────┬──────┐
│ 月份 │ 葡萄 │ 西瓜 │ 荔枝 │ 菠萝 │
├──────┼──────┼──────┼──────┼──────┤
│ Jan  │  12  │  33  │  47  │  21  │
│ Feb  │  41  │  31  │  43  │  19  │
└──────┴──────┴──────┴──────┴──────┘
→ 对应参数: x_col="月份", y_cols=["葡萄","西瓜","荔枝","菠萝"]

格式 B — 长格式(Long Format,有分组字段):
┌──────┬────────────┬──────┐
│ 月份 │ 品类       │ 销量 │
├──────┼────────────┼──────┤
│ Jan  │ 葡萄       │  12  │
│ Jan  │ 西瓜       │  33  │
│ Feb  │ 葡萄       │  41  │
└──────┴────────────┴──────┘
→ 对应参数: x_col="月份", y_col="销量", color_col="品类"

【任务】

1. 更新 src/tools.py 中 make_chart 工具的 Function Calling Schema

   在现有参数基础上新增:
   
   "y_cols": {
       "type": "array",
       "items": {"type": "string"},
       "description": "多系列 Y 轴字段列表(宽格式数据)。\n当需要在同一图表中对比多个数值列时使用,如 ['Q1销售额','Q2销售额','Q3销售额']。\n与 y_col 二选一:单系列用 y_col,多系列用 y_cols。\n注意:y_cols 中的每个字段都必须是数值类型。"
   }
   
   同时更新 y_col 的 description:
   "y_col": {
       "type": "string",
       "description": "Y 轴字段名(单系列时使用)。多系列时改用 y_cols。"
   }
   
   required 中 y_col 改为可选(不再出现在 required 列表里),
   但 y_col 和 y_cols 必须至少提供一个(在函数实现里校验)。

2. 更新 src/tools.py 中 make_chart 函数的实现

   在函数开头加入系列模式判断:
   
   def make_chart(chart_type, x_col, title, x_axis_label, y_axis_label,
                  insight, result_key=None, y_col=None, y_cols=None,
                  color_col=None, y_col_2=None, format_options=None, **kwargs):
       
       # 从 session_state 取数据(保持原有逻辑不变)
       key = result_key or st.session_state.get('latest_query_key')
       data_info = st.session_state['query_results'][key]
       df = data_info['df'].copy()
       
       # 判断系列模式
       is_multi_series = (y_cols is not None and len(y_cols) > 0)
       
       if not is_multi_series and y_col is None:
           st.error("make_chart 必须提供 y_col(单系列)或 y_cols(多系列)之一")
           return
       
       # 字段存在性校验
       if is_multi_series:
           missing = [c for c in y_cols if c not in df.columns]
           if missing:
               st.error(f"以下字段不存在: {missing},可用字段: {list(df.columns)}")
               return
           # 确认所有 y_cols 都是数值类型
           non_numeric = [c for c in y_cols
                          if not pd.api.types.is_numeric_dtype(df[c])]
           if non_numeric:
               st.error(f"y_cols 中的以下字段不是数值类型: {non_numeric}")
               return
       
       # 图表类型分发(只改 bar / barh / line 三种,其他保持不变)
       if chart_type == "bar":
           if is_multi_series:
               fig = _make_bar_multi(df, x_col, y_cols, title,
                                      x_axis_label, y_axis_label)
           else:
               fig = _make_bar_single(df, x_col, y_col, color_col,
                                       title, x_axis_label, y_axis_label)
       
       elif chart_type == "barh":
           if is_multi_series:
               fig = _make_barh_multi(df, x_col, y_cols, title,
                                       x_axis_label, y_axis_label)
           else:
               fig = _make_barh_single(df, x_col, y_col, color_col,
                                        title, x_axis_label, y_axis_label)
       
       elif chart_type == "line":
           if is_multi_series:
               fig = _make_line_multi(df, x_col, y_cols, title,
                                       x_axis_label, y_axis_label)
           else:
               fig = _make_line_single(df, x_col, y_col, color_col,
                                        title, x_axis_label, y_axis_label)
       
       # ... 其他图表类型(pie/donut/scatter/heatmap/area/combo/table)保持原有实现
       
       # 统一渲染(不变)
       st.plotly_chart(fig, use_container_width=True)
       st.info(f"💡 {insight}")
       with st.expander("查看原始数据"):
           st.dataframe(df)

3. 实现四个新的私有函数

   请实现以下四个函数(另外四个 _single 版本保持原有逻辑不变):
   
   def _make_bar_multi(df, x_col, y_cols, title, x_axis_label, y_axis_label):
       """
       多系列柱状图(分组柱状图)
       
       实现方式: px.bar 的 barmode 参数
       - barmode="group": 并排分组柱(默认,对比时用)
       - barmode="stack": 堆叠柱(构成分析时用)
       
       使用 px.bar 的 y 参数传入列表来实现多系列:
       fig = px.bar(
           df,
           x=x_col,
           y=y_cols,          # 传列表,Plotly 自动为每列生成一个系列
           title=title,
           barmode="group",   # 并排分组
           labels={"value": y_axis_label, x_col: x_axis_label},
           color_discrete_sequence=COLOR_PALETTE
       )
       
       然后统一图例名称(去掉 Plotly 自动加的 "variable=" 前缀):
       fig.update_layout(legend_title_text="")
       
       返回 fig
       """
       pass
   
   def _make_barh_multi(df, x_col, y_cols, title, x_axis_label, y_axis_label):
       """
       多系列横向条形图
       
       和 _make_bar_multi 类似,但需要转置坐标:
       fig = px.bar(
           df,
           y=x_col,           # 注意: 横向时 x/y 互换
           x=y_cols,
           title=title,
           barmode="group",
           orientation="h",
           labels={"value": x_axis_label, x_col: y_axis_label},
           color_discrete_sequence=COLOR_PALETTE
       )
       返回 fig
       """
       pass
   
   def _make_line_multi(df, x_col, y_cols, title, x_axis_label, y_axis_label):
       """
       多系列折线图
       
       fig = px.line(
           df,
           x=x_col,
           y=y_cols,          # 传列表
           title=title,
           markers=True,      # 显示数据点标记
           labels={"value": y_axis_label, x_col: x_axis_label},
           color_discrete_sequence=COLOR_PALETTE
       )
       
       加粗线条:
       fig.update_traces(line=dict(width=2))
       fig.update_layout(legend_title_text="")
       
       返回 fig
       """
       pass

4. 更新 src/validators.py 中的多系列校验

   在 validate_chart_type 函数里,增加对 y_cols 的校验:
   
   新增参数: y_cols: list[str] | None = None
   
   新增规则:
   - 如果 y_cols 长度 > 15:
     返回警告"系列数量({N})过多,图表可能难以阅读,建议不超过 6 个系列"(不降级,只警告)
   - 如果 chart_type == "pie" 且 y_cols 不为 None:
     返回降级"饼图不支持多系列,已自动切换为分组柱状图(bar)"
   - 如果 chart_type == "scatter" 且 y_cols 不为 None:
     返回降级"散点图不支持多系列 y_cols,请使用 color_col 区分系列,已切换为 bar"

5. 更新 src/prompts.py 的 AGENT_SYSTEM_PROMPT

   在图表选择规则里加入多系列说明:
   
   多系列图表使用规则:
   当用户问"对比 A、B、C 三个指标的变化"时,使用 y_cols=["A","B","C"]
   当数据是宽格式(每列是一个类别)时,使用 y_cols 传入列名列表
   当数据是长格式(有分类列 + 数值列)时,使用 y_col + color_col
   y_col 和 y_cols 不要同时填写,选其一
   
   典型场景对应:
   宽格式查询结果 + 多列对比 → y_cols=[列名列表]
   长格式查询结果 + 分类着色 → y_col=数值列, color_col=分类列
   单列数据 → y_col=列名(原有用法不变)

【约束】

- 只改 bar / barh / line 三种图表类型,其他图表类型(pie/donut/scatter/
  heatmap/area/combo/table/wordcloud/funnel/bubble)的实现完全不改动
- _make_bar_single / _make_barh_single / _make_line_single 四个单系列函数
  保持原有逻辑不变,只是把原来的代码提取到这四个私有函数里
- COLOR_PALETTE 保持原有定义不变,直接复用
- 图例标题统一设为空字符串(legend_title_text=""),Plotly 默认会显示
  "variable"这个不友好的标题
- 多系列时 format_options 里的 sort_by 参数暂不支持(直接忽略,不报错)

【交付方式】

1. 先给我看更新后的 make_chart 工具 Schema(JSON 部分)
   我确认参数设计合理后再写实现

2. 然后实现四个新的私有函数(_make_bar_multi / _make_barh_multi /
   _make_line_multi,_make_line_single 可以顺手抽出)

3. 然后更新 validators.py 的多系列校验

4. 最后给我 AGENT_SYSTEM_PROMPT 中图表规则的更新片段

5. 写完后提供三个测试用的调用示例:
   - bar 多系列: 各月份 Q1/Q2/Q3/Q4 订单对比
   - line 多系列: 各品类全年销售趋势
   - barh 多系列: 各州在不同支付方式的订单量对比

每完成一步停下来等我确认,不要一次输出所有代码。