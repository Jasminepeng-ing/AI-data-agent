功能优化： 隐藏Olist数据，实现完全使用自己数据分析
我需要对 AI 数据分析 Agent 的数据管理功能做一次架构级重构。

【核心设计原则】

删除操作不能真正修改 DuckDB 文件。
所有"删除"都是会话级隐藏(Session-Level Hiding):
- 底层数据库文件 olist.db 始终保持完整不变
- 用户"删除"某张表时,只是把表名加入当前会话的隐藏列表
- 所有数据访问层方法(list_tables/get_schema/query)都要过滤隐藏表
- 关闭页面/刷新后,session_state 重置,所有数据自动恢复

【Sidebar 新 UI 设计】

─────────────────────────────────────────
  当前数据表
─────────────────────────────────────────
  🗄️ orders       内置  99.4K行    [×]
  🗄️ customers    内置  99.4K行    [×]
  🗄️ order_items  内置  112.6K行   [×]
  ...
  📄 my_data.xlsx 上传  1,234行    [×]
─────────────────────────────────────────
  [🗑️ 清除所有数据]  [📦 仅隐藏Olist]
─────────────────────────────────────────
  ℹ️ 数据仅在本次会话中隐藏,刷新页面后恢复
─────────────────────────────────────────
  📁 上传数据文件
─────────────────────────────────────────

【当前代码结构说明】

- src/data_source.py: DataSource 类
  - self.con: DuckDB 连接
  - list_tables() -> list[str]
  - get_schema() -> str
  - query(sql) -> pd.DataFrame
  - load_uploaded_file(file, table_name) -> str

- src/app.py: Streamlit 主程序
  - Sidebar 有文件上传和数据表展示

- src/agent.py: Agent 主循环
  - run_agent(user_message, history) -> str

- st.session_state 现有键:
  - 'query_results', 'latest_query_key',
    'chart_registry', 'messages'

【任务一: 定义会话隐藏机制的常量和辅助函数】

在 data_source.py 顶部定义:

# Olist 内置表的完整列表
OLIST_BUILTIN_TABLES: set[str] = {
    'orders', 'customers', 'order_items', 'products', 'sellers',
    'order_payments', 'order_reviews', 'geolocation',
    'product_category_name_translation'
}

在 app.py 中定义会话隐藏状态的管理函数:

def get_hidden_tables() -> set[str]:
    """
    获取当前会话中被隐藏的表名集合。
    session_state 没有该键时返回空集合。
    """
    return set(st.session_state.get('hidden_tables', set()))

def hide_table(table_name: str):
    """把单张表加入隐藏列表(不修改数据库)"""
    if 'hidden_tables' not in st.session_state:
        st.session_state['hidden_tables'] = set()
    st.session_state['hidden_tables'].add(table_name)

def hide_all_tables(data_source: DataSource):
    """
    隐藏当前所有可见表(包括内置和上传的)。
    从数据库实际表列表获取,而不是从 list_tables() 获取
    (因为 list_tables 已经过滤了隐藏表)。
    """
    all_real_tables = _get_real_all_tables(data_source)
    st.session_state['hidden_tables'] = set(all_real_tables)

def hide_olist_tables(data_source: DataSource):
    """
    只隐藏 Olist 内置表,保留用户上传的表。
    """
    if 'hidden_tables' not in st.session_state:
        st.session_state['hidden_tables'] = set()
    # 只隐藏数据库中真实存在且属于 Olist 的表
    all_real_tables = _get_real_all_tables(data_source)
    olist_to_hide = all_real_tables & OLIST_BUILTIN_TABLES
    st.session_state['hidden_tables'].update(olist_to_hide)

def _get_real_all_tables(data_source: DataSource) -> set[str]:
    """
    直接从 DuckDB 获取所有实际存在的表(不过滤隐藏状态)。
    用于隐藏/恢复操作,绕过过滤逻辑。
    """
    try:
        rows = data_source.con.execute("SHOW TABLES").fetchall()
        return {row[0] for row in rows}
    except Exception:
        return set()

【任务二: 改造 DataSource 类使其感知隐藏状态】

关键设计:DataSource 类本身不持有 session_state,
而是接收一个外部传入的 hidden_tables 参数。
这样 DataSource 可以独立测试,不与 Streamlit 耦合。

修改以下方法,增加 hidden_tables 参数:

def list_tables(self, hidden_tables: set[str] = None) -> list[str]:
    """
    返回当前可见的表名列表(过滤掉 hidden_tables 中的表)。
    
    实现:
    hidden = hidden_tables or set()
    try:
        rows = self.con.execute("SHOW TABLES").fetchall()
        return [row[0] for row in rows if row[0] not in hidden]
    except Exception:
        return []

def get_schema(self, hidden_tables: set[str] = None) -> str:
    """
    返回可见表的 schema 文本(过滤隐藏表)。
    格式保持原有不变,只是跳过 hidden_tables 中的表。
    """
    hidden = hidden_tables or set()
    # 原有实现基础上,在每张表的循环里加:
    # if table_name in hidden: continue
    pass  # 保持原有实现,加过滤逻辑

def get_table_info(self, hidden_tables: set[str] = None) -> list[dict]:
    """
    返回每张可见表的详细信息(用于 UI 展示)。
    
    返回格式:
    [
        {
            'name': 'orders',
            'type': 'builtin',    # 'builtin' 或 'uploaded'
            'row_count': 99441,
            'col_count': 8,
            'columns': ['order_id', ...]
        }
    ]
    
    实现:
    hidden = hidden_tables or set()
    result = []
    for table in self.list_tables(hidden_tables=hidden):
        try:
            row_count = self.con.execute(
                f'SELECT COUNT(*) FROM "{table}"'
            ).fetchone()[0]
            columns = [col[0] for col in
                       self.con.execute(f'DESCRIBE "{table}"').fetchall()]
            result.append({
                'name': table,
                'type': 'builtin' if table in OLIST_BUILTIN_TABLES
                         else 'uploaded',
                'row_count': row_count,
                'col_count': len(columns),
                'columns': columns
            })
        except Exception:
            continue
    return result

def query(self, sql: str, hidden_tables: set[str] = None) -> pd.DataFrame:
    """
    执行 SQL 前检查是否引用了隐藏表。
    
    实现:
    hidden = hidden_tables or set()
    if hidden:
        sql_lower = sql.lower()
        for table in hidden:
            # 检测 SQL 中是否出现了隐藏表名
            # 用词边界匹配,避免误判(如 'order' 匹配到 'order_items')
            pattern = r'\b' + re.escape(table.lower()) + r'\b'
            if re.search(pattern, sql_lower):
                raise ValueError(
                    f"表 '{table}' 在当前会话中已被隐藏,无法查询。\n"
                    f"如需使用该表,请刷新页面恢复数据。"
                )
    return self.con.execute(sql).df()

【任务三: 重构 app.py 中 Sidebar 的调用方式】

所有调用 data_source 方法的地方都要传入 hidden_tables:

在 app.py 顶部(每次页面渲染时执行):
hidden = get_hidden_tables()  # 从 session_state 读取

然后所有调用改为:
- data_source.list_tables(hidden_tables=hidden)
- data_source.get_schema(hidden_tables=hidden)
- data_source.get_table_info(hidden_tables=hidden)
- data_source.query(sql, hidden_tables=hidden)

【任务四: 实现 Sidebar 数据管理 UI】

完整实现以下 UI:

def render_data_management_sidebar(data_source: DataSource):
    """
    渲染 Sidebar 的数据管理区域。
    包含: 数据表列表 + 两个隐藏按钮 + 上传组件
    """
    hidden = get_hidden_tables()
    table_infos = data_source.get_table_info(hidden_tables=hidden)
    all_real_tables = _get_real_all_tables(data_source)
    
    st.sidebar.markdown("**当前数据表**")
    
    # ── 1. 数据表列表 ─────────────────────────────────
    if not table_infos:
        st.sidebar.info(
            "📭 **当前无可见数据**\n\n"
            "所有数据表已在本次会话中隐藏。\n"
            "刷新页面可恢复所有数据。"
        )
    else:
        for info in table_infos:
            col_icon, col_name, col_meta, col_del = \
                st.sidebar.columns([0.25, 2.0, 1.8, 0.35])
            
            with col_icon:
                icon = "🗄️" if info['type'] == 'builtin' else "📄"
                st.write(icon)
            
            with col_name:
                st.write(f"**{info['name']}**")
            
            with col_meta:
                row_str = _format_row_count(info['row_count'])
                badge_color = (
                    "#1B3A6B" if info['type'] == 'builtin'
                    else "#E87722"
                )
                badge_text = (
                    "内置" if info['type'] == 'builtin' else "上传"
                )
                st.markdown(
                    f'<span style="background:{badge_color};color:white;'
                    f'font-size:10px;padding:1px 6px;border-radius:3px;'
                    f'font-weight:600">{badge_text}</span> '
                    f'<span style="font-size:11px;color:#6B7280">'
                    f'{row_str}</span>',
                    unsafe_allow_html=True
                )
            
            with col_del:
                if st.button(
                    "×",
                    key=f"hide_table_{info['name']}",
                    help=f"在本次会话中隐藏表 {info['name']}"
                ):
                    st.session_state['pending_hide_table'] = info['name']
    
    # ── 单表隐藏确认 ──────────────────────────────────
    pending = st.session_state.get('pending_hide_table')
    if pending:
        st.sidebar.warning(
            f"隐藏表 **{pending}**?\n\n"
            f"本次会话中将无法查询该表,刷新页面后恢复。"
        )
        col_y, col_n = st.sidebar.columns(2)
        with col_y:
            if st.button("确认隐藏", key="confirm_hide_single",
                         type="primary", use_container_width=True):
                hide_table(pending)
                _sync_state_after_hide([pending])
                st.session_state.pop('pending_hide_table', None)
                st.sidebar.toast(f"✅ {pending} 已在本会话中隐藏")
                st.rerun()
        with col_n:
            if st.button("取消", key="cancel_hide_single",
                         use_container_width=True):
                st.session_state.pop('pending_hide_table', None)
                st.rerun()
    
    # ── 2. 展开字段详情 ───────────────────────────────
    if table_infos:
        with st.sidebar.expander("查看字段详情", expanded=False):
            for info in table_infos:
                st.markdown(f"**{info['name']}** ({info['col_count']}列)")
                preview_cols = info['columns'][:6]
                more = info['col_count'] - 6
                suffix = f"...等{info['col_count']}列" if more > 0 else ""
                st.caption("、".join(preview_cols) + suffix)
    
    # ── 3. 两个隐藏按钮 ──────────────────────────────
    st.sidebar.divider()
    
    col_all, col_olist = st.sidebar.columns(2)
    
    with col_all:
        # 没有任何可见表时禁用
        all_disabled = len(table_infos) == 0
        if st.button(
            "🗑️ 清除所有",
            key="btn_hide_all",
            help="在本次会话中隐藏所有数据表(刷新后恢复)",
            disabled=all_disabled,
            use_container_width=True
        ):
            st.session_state['confirm_hide_all'] = True
    
    with col_olist:
        # 没有可见的 Olist 表时禁用
        visible_olist = [t for t in table_infos
                         if t['type'] == 'builtin']
        olist_disabled = len(visible_olist) == 0
        if st.button(
            "📦 仅隐藏Olist",
            key="btn_hide_olist",
            help="只隐藏 Olist 内置数据,保留你上传的文件(刷新后恢复)",
            disabled=olist_disabled,
            use_container_width=True
        ):
            st.session_state['confirm_hide_olist'] = True
    
    # ── 清除所有确认 ──────────────────────────────────
    if st.session_state.get('confirm_hide_all'):
        st.sidebar.warning("⚠️ 将隐藏所有数据表,包括你上传的文件")
        col_y, col_n = st.sidebar.columns(2)
        with col_y:
            if st.button("确认", key="confirm_all_yes",
                         type="primary", use_container_width=True):
                names_before = [t['name'] for t in table_infos]
                hide_all_tables(data_source)
                _sync_state_after_hide(names_before)
                _insert_system_message(
                    "🗑️ **所有数据表已在本次会话中隐藏。**\n\n"
                    "当前没有可查询的数据。请上传你的数据文件,\n"
                    "或刷新页面恢复 Olist 内置数据集。"
                )
                st.session_state.pop('confirm_hide_all', None)
                st.rerun()
        with col_n:
            if st.button("取消", key="confirm_all_no",
                         use_container_width=True):
                st.session_state.pop('confirm_hide_all', None)
                st.rerun()
    
    # ── 仅隐藏 Olist 确认 ─────────────────────────────
    if st.session_state.get('confirm_hide_olist'):
        uploaded_count = len([t for t in table_infos
                               if t['type'] == 'uploaded'])
        st.sidebar.info(
            f"将隐藏 {len(visible_olist)} 张 Olist 内置表,\n"
            f"保留 {uploaded_count} 张上传文件"
        )
        col_y, col_n = st.sidebar.columns(2)
        with col_y:
            if st.button("确认", key="confirm_olist_yes",
                         type="primary", use_container_width=True):
                olist_names = [t['name'] for t in visible_olist]
                hide_olist_tables(data_source)
                _sync_state_after_hide(olist_names)
                _insert_system_message(
                    "📦 **Olist 内置数据已在本次会话中隐藏。**\n\n"
                    "你上传的文件仍然可用。\n"
                    "刷新页面可恢复 Olist 数据。"
                )
                st.session_state.pop('confirm_hide_olist', None)
                st.rerun()
        with col_n:
            if st.button("取消", key="confirm_olist_no",
                         use_container_width=True):
                st.session_state.pop('confirm_hide_olist', None)
                st.rerun()
    
    # ── 4. 会话说明提示 ──────────────────────────────
    if hidden:
        hidden_count = len(hidden)
        st.sidebar.caption(
            f"ℹ️ 本次会话已隐藏 {hidden_count} 张表。"
            f"刷新页面后自动恢复。"
        )
    
    # ── 5. 文件上传组件 ──────────────────────────────
    st.sidebar.divider()
    uploaded_files = st.sidebar.file_uploader(
        "📁 上传数据文件",
        type=['xlsx', 'csv'],
        accept_multiple_files=True,
        help="支持 Excel(.xlsx)和 CSV 文件,上传后立即可用"
    )
    if uploaded_files:
        for f in uploaded_files:
            table_name = data_source.load_uploaded_file(f)
            if table_name:
                # 如果上传的文件曾经被隐藏,自动从隐藏列表移除
                if table_name in get_hidden_tables():
                    st.session_state['hidden_tables'].discard(table_name)
                st.sidebar.success(f"✅ {f.name} → 表 `{table_name}`")

【任务五: 实现 session_state 同步辅助函数】

def _sync_state_after_hide(hidden_table_names: list[str]):
    """
    隐藏一批表后,清理 session_state 中引用了这些表的缓存。
    不清除 messages(保留对话历史)。
    """
    hidden_set = set(hidden_table_names)
    
    # 清理 query_results 中引用了被隐藏表的条目
    query_results = st.session_state.get('query_results', {})
    keys_to_remove = []
    for key, val in query_results.items():
        sql = val.get('sql', '').lower()
        for table in hidden_set:
            pattern = r'\b' + re.escape(table.lower()) + r'\b'
            if re.search(pattern, sql):
                keys_to_remove.append(key)
                break
    for key in keys_to_remove:
        query_results.pop(key, None)
    
    # 如果 latest_query_key 指向被移除的缓存,重置
    if st.session_state.get('latest_query_key') in keys_to_remove:
        st.session_state['latest_query_key'] = None
    
    # 清理 chart_registry 中基于被隐藏表数据的图表
    # (用 caption 做模糊匹配,不能100%准确,但尽力清理)
    chart_reg = st.session_state.get('chart_registry', [])
    st.session_state['chart_registry'] = [
        c for c in chart_reg
        if not any(table in c.get('caption', '').lower()
                   for table in hidden_set)
    ]

def _insert_system_message(content: str):
    """
    在对话历史中插入一条 assistant 系统提示消息。
    用于告知用户数据状态变化。
    """
    if 'messages' not in st.session_state:
        st.session_state['messages'] = []
    st.session_state['messages'].append({
        'role': 'assistant',
        'content': content
    })

def _format_row_count(n: int) -> str:
    """把数字格式化: 99441 → '99.4K行'"""
    if n >= 1_000_000:
        return f"{n/1_000_000:.1f}M行"
    elif n >= 1_000:
        return f"{n/1_000:.1f}K行"
    return f"{n}行"

【任务六: 更新 agent.py 的数据状态感知】

在 run_agent 函数开头动态注入当前数据状态:

def run_agent(user_message: str, history: list) -> str:
    import streamlit as st
    
    # 获取当前会话的隐藏表集合
    hidden = set(st.session_state.get('hidden_tables', set()))
    
    # 获取真实可见的表列表
    visible_tables = data_source.list_tables(hidden_tables=hidden)
    
    # 构建数据上下文注入到 system prompt
    if visible_tables:
        data_context = (
            f"\n\n【当前会话可用数据表】\n"
            f"共 {len(visible_tables)} 张表: "
            f"{', '.join(visible_tables)}\n"
            f"查询时只能使用以上表名。"
            + (f"\n本次会话中以下表已被隐藏不可用: "
               f"{', '.join(hidden)}"
               if hidden else "")
        )
    else:
        data_context = (
            "\n\n【当前数据状态】\n"
            "⚠️ 当前会话中没有任何可用的数据表。\n"
            "请直接告知用户: 所有数据在本次会话已被隐藏或尚未上传数据。\n"
            "刷新页面可恢复内置数据,或引导用户上传文件。\n"
            "禁止调用 query_database 工具或生成任何 SQL。"
        )
    
    system_with_context = AGENT_SYSTEM_PROMPT + data_context
    
    messages = [
        {"role": "system", "content": system_with_context},
        *history,
        {"role": "user", "content": user_message}
    ]
    # ... 后续 Agent 循环逻辑不变

【约束】

架构约束:
- 任何情况下都不允许执行 DROP TABLE 语句
- 所有"删除"行为只修改 st.session_state['hidden_tables']
- olist.db 文件始终保持完整,只读不写(除了 load_uploaded_file)

安全约束:
- query() 方法必须在执行前检查 SQL 是否引用了隐藏表
- 检测到引用隐藏表时抛出 ValueError,错误信息要友好:
  "表 'orders' 在当前会话中已被隐藏。刷新页面后可恢复。"
- Agent 在无数据状态下不得调用 query_database 工具

UI 约束:
- 所有按钮必须有两步确认(点击→确认弹框→执行)
- 按钮 key 必须全局唯一
- 隐藏了表之后,Sidebar 底部必须显示"已隐藏 N 张表,刷新恢复"的提示
- "仅隐藏Olist"按钮在所有 Olist 表已隐藏时自动 disabled
- "清除所有"按钮在所有表已隐藏时自动 disabled

上传文件约束:
- 上传新文件后,如果该文件对应的表名曾在隐藏列表里,
  自动从隐藏列表移除(相当于"重新激活")
- 不修改 load_uploaded_file 的返回值格式

【交付方式】

严格按以下顺序,每步完成后停下来等我确认:

Step 1: 在 data_source.py 中改造
        list_tables / get_schema / get_table_info / query
        四个方法,增加 hidden_tables 参数
        写单元测试:
        (a) 正常调用不传 hidden_tables,返回所有表
        (b) 传入 hidden_tables={'orders'},orders 不出现在结果中
        (c) 对隐藏表执行 SQL,触发 ValueError 并打印错误信息

Step 2: 在 app.py 中实现所有辅助函数:
        get_hidden_tables / hide_table / hide_all_tables /
        hide_olist_tables / _get_real_all_tables /
        _sync_state_after_hide / _insert_system_message /
        _format_row_count

Step 3: 实现 render_data_management_sidebar 函数
        完成后确认 UI:
        - 数据表列表显示正确(图标/名称/行数/类型标签/×按钮)
        - 两个操作按钮并排显示
        - 按钮在对应情况下 disabled

Step 4: 端到端测试"仅隐藏Olist"流程:
        (a) 点击"仅隐藏Olist" → 出现确认弹框,显示将隐藏N张内置表
        (b) 确认后 → Olist 表从列表消失,上传文件保留
        (c) Sidebar 底部显示"已隐藏9张表,刷新恢复"
        (d) 对话中出现系统提示消息
        (e) 问 Agent "查一下订单数量" →
            Agent 回复"orders表在当前会话已隐藏"而不是执行 SQL

Step 5: 端到端测试"清除所有"流程:
        (a) 清除后所有表消失,显示空状态引导
        (b) 刷新页面后 Olist 数据恢复,上传文件消失(因为是临时注册的)
        (c) 在 Step 4 已隐藏 Olist 的基础上上传新文件 →
            只显示新文件,Olist 仍然隐藏

Step 6: 测试上传文件"重新激活"逻辑:
        (a) 上传 test.xlsx → 注册为表 test
        (b) 隐藏 test 表
        (c) 重新上传同名 test.xlsx →
            test 表重新出现在列表中(从隐藏列表自动移除)

Step 7: 更新 agent.py 加入数据状态感知
        测试无数据状态下 Agent 的回复是否友好且不调用工具

Step 8: 告诉我以下两个问题的答案:
        (a) 用户用"仅隐藏Olist"隐藏了内置数据,
            然后上传了自己的文件做完分析,
            然后刷新了页面。此时的状态是什么?
            Olist 数据会恢复吗?上传的文件还在吗?
        (b) 如果两个浏览器 Tab 同时打开这个 Agent,
            一个 Tab 隐藏了 Olist,另一个 Tab 会受影响吗?
            (提示:Streamlit 默认每个 Tab 是独立的 session)